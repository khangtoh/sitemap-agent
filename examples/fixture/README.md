# Fixture example

`sitemap.json` is a small local-fixture crawl artifact. Generate its offline
companion visual with:

```bash
bun run bin/sitemap-agent.ts render examples/fixture/sitemap.json --out examples/fixture/visual.html
```
