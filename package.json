{
  "name": "sitemap-agent",
  "version": "0.1.0",
  "devDependencies": {
    "@khangtoh/specloop": "^0.3.1",
    "@types/node": "^26.4.0",
    "bun-types": "^1.4.0",
    "typescript": "^5.6.0"
  },
  "bin": {
    "sitemap-agent": "./bin/sitemap-agent.ts"
  },
  "description": "Crawls a base domain, builds a JSON site graph (not a sitemap.xml), and renders a self-contained interactive visual of it.",
  "private": true,
  "scripts": {
    "check:spec": "bunx --bun specloop check",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "type": "module"
}
